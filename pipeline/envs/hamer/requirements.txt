# Env Python pour HaMeR.
# Python 3.10 imposé. Cf. docs/PIPELINE.md §1.1.
#
# IMPORTANT : PyTorch et detectron2 ne sont PAS installables via pip seul.
# Voir scripts/setup.sh pour la procédure exacte.
#
# 1) PyTorch ≤ 1.13 + cu117 — installé séparément
# 2) detectron2 — pip install git+https://github.com/facebookresearch/detectron2 (ou commit pinné)
# 3) ViTPose — installé en éditable depuis third-party/ViTPose dans le repo cloné
# 4) Le reste ci-dessous

mmcv==1.3.9
gdown
opencv-python
pyrender
pytorch-lightning
scikit-image
smplx==0.1.28
yacs
chumpy @ git+https://github.com/mattloper/chumpy
timm
einops
xtcocotools
pandas
hydra-core
hydra-submitit-launcher
hydra-colorlog
pyrootutils
rich
webdataset

numpy>=1.21,<1.24
pyyaml>=6.0,<7
pygltflib>=1.16,<2
tqdm
